#ifndef HOST_COMPAT_ADAFRUIT_I2CDEVICE_H
#define HOST_COMPAT_ADAFRUIT_I2CDEVICE_H
// Adafruit_GFX.h includes this unconditionally but the GFX core never uses it.
// Empty stub so the host build doesn't need Adafruit_BusIO / Wire.
#endif
